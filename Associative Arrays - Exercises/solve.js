function solve(data) {

    let res = {};
    let dataL = data.length;

    for (let index = 0; index < dataL; index++) {
        if(!res.hasOwnProperty(index)) {
            res
        }
        
    }
    
}

solve('Java C# Php PHP Java PhP 3 C# 3 1 5 C#')